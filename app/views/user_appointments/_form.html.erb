<%= form_with(model: user_appointment) do |form| %>
  <% if user_appointment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_appointment.errors.count, "error") %> prohibited this user_appointment from being saved:</h2>

      <ul>
        <% user_appointment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :patient_user_id, style: "display: block" %>
    <%= form.text_field :patient_user_id %>
  </div>

  <div>
    <%= form.label :appointment_id, style: "display: block" %>
    <%= form.text_field :appointment_id %>
  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
